<h1>Listing private_locations</h1>

<table>
  <tr>
    <th>Owner</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @private_locations.each do |private_location| %>
  <tr>
    <td><%= private_location.owner %></td>
    <td><%= link_to 'Show', private_location %></td>
    <td><%= link_to 'Edit', edit_private_location_path(private_location) %></td>
    <td><%= link_to 'Destroy', private_location, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Private location', new_private_location_path %>
